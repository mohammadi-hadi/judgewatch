<div align="center">

# judgewatch

[![CI](https://github.com/mohammadi-hadi/judgewatch/actions/workflows/test.yml/badge.svg)](https://github.com/mohammadi-hadi/judgewatch/actions/workflows/test.yml)
[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.21806464.svg)](https://doi.org/10.5281/zenodo.21806464)
[![Python](https://img.shields.io/badge/python-3.10%2B-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

*Monthly bias audits of LLM judges.*

</div>

LLM-as-judge is now the default way to evaluate models, agents, and prompts —
but judges have measurable biases, and they drift when providers update models
behind stable API names. judgewatch runs a **frozen probe set** against popular
judge models on a monthly schedule and publishes the results, so you can see
which judge to trust this month and whether yours changed under you.

**Leaderboard:** https://mohammadi.cv/judgewatch/ (first audit pending)

## What it measures

| Probe | Question it answers | Metric |
|---|---|---|
| Position | Does swapping the two answers change the verdict? | flip rate over 16 pairs judged in both orders |
| Verbosity | Does wrapping a correct answer in filler win? | padded-preference rate over 12 items, both orders |
| Bandwagon | Does a fabricated "9 out of 10 experts prefer…" line flip the judge's own verdict? | flip rate vs the judge's clean baseline |
| Consistency | Does the judge agree with itself? | exact-agreement rate over 8 items scored 3 times each |

Parse failures and API errors are excluded from denominators and reported as
their own metric — a judge that can't follow the output format is a finding,
not noise.

## Method

- The probe set (`judgewatch/probes/probeset_v1.yaml`, shipped with the
  package) is frozen and versioned. The same
  36 hand-written items run every month, so results are comparable across
  runs and drift is attributable to the judge, not the test.
- Every pairwise item is judged in **both orders**, which separates position
  effects from content preference. The verbosity probe also runs both orders,
  so it isn't confounded by position bias.
- The bandwagon claim always targets the judge's **own** clean verdict, so a
  flip means the claim moved the judge, not that the claim happened to agree
  with it.
- Judges run with provider-default settings (no temperature or thinking
  overrides) — the audit measures the configuration people actually deploy.
- Everything under `data/` and `docs/` is generated by the pipeline, never
  edited by hand.

The biases probed here are documented in the literature — see Zheng et al.
2023, ["Judging LLM-as-a-Judge with MT-Bench and Chatbot
Arena"](https://arxiv.org/abs/2306.05685). judgewatch's contribution is
longitudinal: the same probes, monthly, across judges.

## Run it yourself

```
git clone https://github.com/mohammadi-hadi/judgewatch
cd judgewatch
make install
make test                       # offline, runs against a deterministic mock

export ANTHROPIC_API_KEY=...    # and/or OPENAI_API_KEY for other endpoints
make audit JUDGES=anthropic:claude-haiku-4-5
make serve                      # leaderboard at http://localhost:8765
```

Judges are configured in `judges.yaml` (all disabled by default so nothing
spends credits by accident). Any Anthropic model or OpenAI-compatible
`/chat/completions` endpoint works.

Installing the package also installs a `judgewatch` command with the same
subcommands:

```
judgewatch run --judges anthropic:claude-haiku-4-5 --workers 8
judgewatch report
judgewatch site
```

Probe calls run 4-way concurrent by default (`--workers`), a judge finishes in
a couple of minutes, and progress is printed as each probe starts.

## Gate your CI on it

`judgewatch check` audits a judge and exits non-zero when a bias metric
breaches a threshold — useful as a scheduled CI job on any pipeline that
depends on an LLM judge:

```
judgewatch check --judges anthropic:claude-haiku-4-5 \
  --max-position-flip 0.25 --max-bandwagon-flip 0.25 --min-consistency 0.6
```

| Threshold | Default | Meaning |
|---|---|---|
| `--max-position-flip` | 0.25 | flip rate when answer order is swapped |
| `--max-verbosity-pref` | 0.65 | padded-answer preference rate |
| `--max-bandwagon-flip` | 0.25 | flips caused by a fabricated consensus |
| `--min-consistency` | 0.60 | exact-agreement rate on repeat scoring |
| `--max-failures` | 0.05 | API errors + unparseable verdicts |

The defaults are starting points, not community norms — tune them to your own
risk tolerance. `--save results.json` keeps the full per-item records.

## Data

Everything the leaderboard shows is also published as JSON:

**https://mohammadi.cv/judgewatch/data.json**

The payload carries the latest run, a `generated_at` timestamp, and the full
month-by-month `history`, so you can build badges, dashboards, or your own
trend analysis on top of it. Raw per-item records (every verdict, every score)
live in the repo under `data/runs/<month>/`.

Per-judge [shields.io](https://shields.io) endpoints are published under
`badges/` — to show a judge's current audit result in your own README:

```
![judgewatch](https://img.shields.io/endpoint?url=https%3A%2F%2Fmohammadi.cv%2Fjudgewatch%2Fbadges%2Fclaude-haiku-4-5.json)
```

(`badges/leader.json` always points at the currently best-ranked judge.)

### Cost

A full audit is 96 calls per judge with short prompts. Assuming roughly 700
input and 200–500 output tokens per call (more for models that think by
default), one judge costs about **$0.15–0.25** on a Haiku-class model and
**under $2** on an Opus-class model at August 2026 API prices — a five-judge
monthly audit is a few dollars.

## Sponsoring

judgewatch is free and MIT-licensed, and the monthly runs are paid out of
pocket. Sponsorship decides how far the audit scales:

- more judges per month (including expensive frontier models),
- larger probe sets and new probes (self-preference is next — see roadmap),
- and the maintenance time to keep results trustworthy.

Sponsors are credited on the leaderboard, and sponsor votes pick which judge
models get added next. Sponsor via [GitHub
Sponsors](https://github.com/sponsors/mohammadi-hadi).

## Roadmap

- Self-preference probe: does a judge favor answers written by its own model
  family? (Needs a generation stage, so it lands once sponsorship covers it.)
- Richer trend charts (the table already shows month-over-month deltas and a
  position-flip sparkline once two runs exist).
- `probeset_v2.yaml` with community-contributed items (v1 stays frozen and
  keeps running for comparability).

## Related projects

- [judgekit](https://github.com/mohammadi-hadi/judgekit) — audit *your own*
  judge pipelines in depth; judgewatch is the public, longitudinal complement.
- [trajectory-judge](https://github.com/mohammadi-hadi/trajectory-judge) —
  evaluating agent trajectories with LLM judges.

## Citing

Releases are archived on Zenodo. Cite the concept DOI
[10.5281/zenodo.21806464](https://doi.org/10.5281/zenodo.21806464), which
always resolves to the latest version; structured metadata is in
[CITATION.cff](CITATION.cff).

## License

MIT — see [LICENSE](LICENSE).
